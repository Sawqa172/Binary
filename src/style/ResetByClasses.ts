import styled from "styled-components";

export const btnReset = `
  border: none;
  min-width: 120px;
  background: transparent;
  cursor: pointer;
`;

export const pReset = `
  padding: 0;
`;

export const aReset = `
  text-decoration:none;
  &:hover{
  text-decoration:none;
 }
`;
